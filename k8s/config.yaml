apiVersion: v1
kind: ConfigMap
metadata:
  name: campus-config
  namespace: default
data:
  BACKEND_HOST: "campus-backend-service"
  BACKEND_PORT: "8000"
  NODE_ENV: "production"
  DB_HOST: "campus-postgres"
  DATABASE_HOST: "campus-postgres"